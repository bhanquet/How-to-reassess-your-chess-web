{
  "name": "test-chess-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "mkdir -p public/data public/cm-chessboard && cp -r data/. public/data/ && cp -r node_modules/cm-chessboard/assets/. public/cm-chessboard/",
    "predev": "mkdir -p public/data public/cm-chessboard && cp -r data/. public/data/ && cp -r node_modules/cm-chessboard/assets/. public/cm-chessboard/",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "cm-chessboard": "^8.13.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
