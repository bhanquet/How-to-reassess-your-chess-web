# How to Reassess Your Chess - Interactive Application

Interactive web application for personal study of the book "How to Reassess Your Chess" (4th Edition) by Jeremy Silman.

> **Copyright:** this repo contains **no book text**. `data/book_structure.json`,
> `data/diagrams.json` and `data/toc.json` are generated locally (gitignored).
> Buy the EPUB, place it at the repo root as
> `How to Reassess Your Chess 4th ed - Silman.epub`, then from the root run
> `python3 -m silman_parser.build` before `npm run dev / build`.

## Features

- **Table of contents navigation**: Quick access to the 9 main parts of the book
- **Section-by-section reading**: Navigate through the book's logical sections
- **Inline diagrams**: Chessboards appear directly in the text next to each diagram
- **Interactive chessboard**: Click "Enlarge" to play through moves and analyze a position
- **Integrated search**: Quickly find concepts or specific positions
- **Keyboard navigation**: Arrow keys ← → to move forward/backward, Esc to close the board

## Project structure

```
docker-compose.yml          # Project root: Docker deployment (image silman-chess-app, port 5174)
chess-app/
├── index.html              # Application entry point (ES modules)
├── vite.config.js          # Vite config (dev server 5173, build, vitest tests)
├── package.json            # npm scripts + dependencies (chess.js, cm-chessboard, vite, vitest)
├── Dockerfile              # Multi-stage build (node:20-alpine -> nginx:alpine)
├── nginx.conf              # Nginx config (port 80, asset cache, SPA fallback)
├── .dockerignore           # Docker context exclusions (node_modules, dist, public)
├── css/
│   ├── base.css            # Layout, sidebar, typography (light-dark() theme)
│   ├── diagrams.css        # Inline diagrams + game notations
│   ├── modal.css           # Enlarged modal chessboard
│   ├── components.css      # Welcome, navigation, search
│   └── responsive.css      # Media queries + print
├── js/
│   ├── config.js           # Constants (starting FEN, limits)
│   ├── chess-utils.js      # Pure chess logic (variations, validation)
│   ├── templates.js        # HTML builders (sections, diagrams, search)
│   ├── navigation.js       # TOC + sidebar
│   ├── search.js           # Full-text search
│   ├── playable-board.js   # Shared inline/modal core (chess.js v1 + cm-chessboard v8)
│   ├── inline-boards.js    # Inline diagrams (lazy via IntersectionObserver)
│   ├── modal-board.js      # Modal chessboard (ModalBoardManager)
│   └── app.js              # Orchestrator (ChessApp)
├── data/
│   ├── book_structure.json # GENERATED, gitignored — run python3 -m silman_parser.build from root
│   ├── diagrams.json       # GENERATED, gitignored (FEN, moves)
│   ├── toc.json            # GENERATED, gitignored
│   └── diagrams_manual_overrides.json # versioned fixes
├── public/                 # Regenerated by prebuild/predev (data/, cm-chessboard/)
├── tests/                  # vitest tests (chess-utils, search)
└── README.md               # This file
```

## How to use

### 0. Generate the book data (once, from the repo root)

```bash
# 1. Buy the EPUB legally, place it at the repo root as:
#    How to Reassess Your Chess 4th ed - Silman.epub
# 2. Then:
python3 -m silman_parser.build
```

### Start the application (after step 0)

1. Install dependencies and start the Vite dev server:
   ```bash
   cd chess-app
   npm install
   npm run dev
   ```

2. Open your browser and go to:
   ```
   http://localhost:5173
   ```

3. Production build (optional):
   ```bash
   npm run build      # generates dist/ (public/ is regenerated by prebuild)
   npm run preview    # serves the build (http://localhost:4173)
   ```

### Navigation

- **Side menu** (☰): Click to open/close the navigation menu
- **Table of contents**: Click a part or chapter to jump directly to it
- **Navigation buttons**: Use "← Previous" and "Next →" to move through the book
- **Progress bar**: Visualize your progress through the book

### Inline diagrams

Each diagram in the book appears directly in the text as an interactive chessboard.

- Positions come from the integrated PGN studies in `data/diagrams.json`
- Use the buttons under the board to navigate through recorded moves
- Click "Enlarge" to open the diagram in modal mode
- Diagrams with unknown positions display a "default position" badge

### Search

- Use the search bar at the top right
- Type a keyword (e.g. "Knight", "bishop", "pawn structure")
- Click a result to navigate to that passage

## Docker

Generate the data locally first (`python3 -m silman_parser.build` from the root):
the Dockerfile does NOT embed the EPUB. Then from the project root:

```bash
docker compose up -d --build
```

Then open: http://localhost:5174

## Keyboard shortcuts

- `←` (Left arrow): Previous section
- `→` (Right arrow): Next section
- `Esc`: Close modal chessboard
- `Home`/`End`: First/last position of a diagram
- `←`/`→` (with focused diagram): Navigate through moves

## Book data

- **Total sections**: one per EPUB H2 (132 after splitting sections > 15,000 characters)
- **Unique diagrams**: 432 (714 occurrences in the text)
- **FEN coverage**: 100 % (429 via Study parsing + 3 via exclusive manual overrides, 4 overrides total; diagrams without FEN would show the starting position with a warning badge)
- **Diagrams with navigable moves**: 299 (+ 235 with Study variations)
- **Book text**: NOT included in git — generated locally from your purchased EPUB

## Book parts

1. **Part One**: The Concept of Imbalances
2. **Part Two**: Minor Pieces (Knights, Bishops, Bishops vs. Knights)
3. **Part Three**: Rooks
4. **Part Four**: Psychological Meanderings
5. **Part Five**: Target Consciousness
6. **Part Six**: Statics vs. Dynamics
7. **Part Seven**: Space
8. **Part Eight**: Passed Pawns
9. **Part Nine**: Other Imbalances

## Data generation scripts

- `silman_parser/` : parser package (one module per responsibility:
  `epub_ingest` (EPUB source, spine order), `san`, `segmentation`,
  `html_render`, `diagram_context`, `study`, `overrides`, `build`)

To regenerate the data (from the project root, EPUB required — see §0):
```bash
python3 -m silman_parser.build
```

## Tests

Frontend (vitest, from `chess-app/`):
```bash
npm test
```
- `tests/chess-utils.test.js` : chess logic (variations, validation)
- `tests/search.test.js` : full-text search

Backend (parser + JSON integrity, stdlib only, from the root):
```bash
python3 -m unittest discover -s tests -v
```

## Dependencies

- chess.js ^1.4.0 (npm, ESM modules)
- cm-chessboard ^8.13.0 (npm, ESM modules) — CSS and pieces copied into
  `public/cm-chessboard/` by the prebuild/predev scripts; pieces are
  loaded from `public/cm-chessboard/pieces/staunty.svg`
- vite ^5.4 (build + dev server) and vitest ^1.6 (tests)

No more CDN or `lib/` folder: everything goes through `npm install`.
`img/chesspieces/wikipedia/` (old chessboard.js pieceTheme) has been removed.

## Source of the text (EPUB)

The text comes from the native EPUB **How to Reassess Your Chess 4th ed - Silman.epub**,
read in the order of the `content.opf` spine. Sections are the H2s (H3s rendered as
`<h3>` in the content), diagram blocks (`<p class="diagram-number">Diagram N</p>`)
become `<div class="diagram-inline" data-diagram="N">`, and `<span class="bold">`
move spans become `<div class="game-notation">`. FENs come from the PGN studies
(`pgn_studies/`), not from the EPUB.

## License

App code shareable. Book © Jeremy Silman — no book text in this repo; each user
generates it locally from their legally purchased EPUB.
